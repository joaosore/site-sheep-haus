import $ from "jquery";

$(document).ready(function() {
    
    var loginErrors = $('.login-form').find('.invalid-feedback');
    // alert(`has ${loginErrors.length}`);

    // if (loginErrors.length > 0) {
    //     @foreach ($vacancies as $vacancy)
    // }

});